# Continuidad — Sesión 13, cierre nocturno: SITIO PÚBLICO CON SÍMBOLO PROPIO

> Documento de continuidad (snapshot fechado). **Este reemplaza al
> `CONTINUIDAD_2026-08-09_1903.md` como punto de arranque**: aquel se
> escribió antes de la producción del logo y ya no refleja el estado. El de
> las 19:03 se conserva como registro del momento del switch.
>
> No reemplaza a `PLAN-MAESTRO-v2.md`, `PENDIENTES.md` ni
> `ERRORES-Y-APRENDIZAJES.md`; los resume para arranque rápido.

**Snapshot creado:** domingo 9 de agosto de 2026, 22:08 (hora Ecuador, UTC-5)
**Autor:** Claude Code (cierre nocturno de la Sesión 13)
**Cubre:** el día completo — switch a público (18:40) y la noche de producción de marca.

---

## 1. Estado: qué está vivo

| Qué | Dónde | Estado |
|---|---|---|
| **Sitio público** | `barreraglobal.com` y `www` | **VIVO desde las 18:40**, 12 páginas reales vía `reverse_proxy sitio-bg-web:8080`. |
| **Símbolo propio** | favicon, header, footer | **DESPLEGADO Y VERIFICADO EN PRODUCCIÓN**: `favicon.svg` 200, `logo.svg` 200, container healthy, **Gate 0 5/5**. Se acabó el logo de Astro. |
| Staging | `staging.barreraglobal.com` | Vivo y con candado (401). |
| Repo | `fbarrerainversiones/sitio-bg-infra` | `main` con el cierre nocturno; tree limpio. |
| Aurora | `/opt/stack/` | **Intacta.** Ningún incidente del día la tocó. Downtime acumulado sigue siendo los ~5 min de Sesión 9. |

**Manual de Marca v3.0:** entregado, **vive fuera del repo**. Misión, visión y valores en estado **PROPUESTA**, pendientes del veredicto de Francisco (**P-56**).

---

## 2. Lo que pasó hoy, en una pantalla

**Tarde — el switch.** Fix de la 404 (`8c7c18a`, cierra P-42 como R-17), cierre legal con la aprobación del abogado y retiro del disclaimer (`6772a51`, `35e4138`, cierra P-39 como R-18), y el switch: **v1 falló** porque `sed -i` reemplazó el inodo del Caddyfile bind-monteado y el caddy validó y recargó la config **vieja** reportando éxito (**E-27**, delatado por `/no-existe` devolviendo **200**); **v2 funcionó** con edición inode-preserving y `docker restart`; y después el bloque de rollback «solo si falla» se pegó igual, devolviendo el cartel viejo ~10 min (**NM-11**). Reglas nuevas: **R-48**, **R-49**, **R-50**.

**Noche — la marca.** Producción y deploy de la fase 1 de P3 (`d38da29`, `5f9e65d`, `3d84f01`, `20f40ac` + rebuild). De paso se cerraron dos fantasmas: el favicon de Astro que se servía en la pestaña de cada visitante, y el campo `logo` del JSON-LD que apuntaba a un archivo inexistente en las 12 páginas.

**⚠️ La lección de la noche:** el **Purge de Cloudflare se ejecutó ANTES** de desplegar los assets, así que el borde recacheó lo viejo y siguió entregando el favicon de Astro **~2 h 35 min** (18:40–21:15) con el origen ya sirviendo el nuevo. **El Purge va SIEMPRE DESPUÉS del deploy**, y se verifica **en incógnito**.

---

## 3. Mañana, en orden

| # | Qué | Quién | Nota |
|---|---|---|---|
| **a** | **Purge #2 + verificar el favicon en incógnito**, si no se hizo ya | Francisco | Es el cierre de la lección de caché. En incógnito, no en la ventana de siempre. |
| **b** | **Aviso a la sesión de Aurora** | Francisco | Bloque en el chat, **baseline 5754**. |
| **c** | **Correo `privacidad@` probado** | Francisco | **Es lo único que cierra P-51.** Configurar sin probar no cuenta. El sitio ya publica esa dirección 4 veces con plazo de 15 días. |
| **d** | **Veredicto de misión / visión / valores** (**P-56**) | Francisco | Están en PROPUESTA. De esta capa cuelga todo el copy futuro. |
| **e** | **Prompt de F1 — el pórtico animado** (**P-54**) | Auditor → Claude | SVG por scroll, **CSS puro, cero JS** → cero hashes CSP nuevos. Geometría **oficial del repo**. |
| **f** | **Handoff al manager** → reloj **48 h** → reels | Francisco | |
| **g** | **Espejo del knowledge** al cierre | Francisco | Borrar la versión vieja **antes** de subir la nueva (E-17 / R-17). |

---

## 4. Numeración vigente (leída de los archivos)

```
Errores:      28 (E-28)        Near-miss:  11 (NM-11)
Reglas:       50 (R-50)        Pendientes: 56 (P-56)
Resueltos:    18 (R-18)        Decisiones: D-29
CLAUDE.md v2.4  ·  ERRORES-Y-APRENDIZAJES.md v2.3
```

**Altas de la noche:** **P-54** (experiencia visual del home en 4 fases), **P-55** (recuadro opcional del `logo.svg` para el panel de Google), **P-56** (aprobar misión/visión/valores).

**Trampa conocida:** la §10 del Plan Maestro lista D-18 a D-24 y D-29; **D-25 a D-28 viven en la bitácora** (Sesiones 6 y 8). No asumir que la §10 tiene todas.

---

## 5. Recordatorios que ahorran una noche

- **Todo cambio de assets o config viaja en la imagen: exige rebuild** (R-44). El `git pull` no basta.
- **Claude Code pushea primero, el VPS jala después** (R-47). Confirmar por hash antes del rebuild.
- **Nada de `sed -i` ni `mv` sobre archivos bind-monteados** (R-48). Comparar `ls -i` host vs container ante la duda.
- **Los bloques condicionales de un runbook solo se ejecutan si su condición se cumple** (R-49).
- **Después de tocar el proxy, pedir una URL inventada:** si `/no-existe` da **200**, no estás mirando el sitio real.
- **Nada interno en `web/public/`**: se copia entero a `dist/` y se sirve. El build debe dar exactamente **12** archivos `.html`.

---

**Fin del snapshot.**

**Siguiente snapshot:** al cierre de la próxima sesión.
